{{ form_start(form) }}
    {{ form_widget(form) }}
        <button id="vehiculos-button" class="btn btn-block btn-success submit-button" type="submit">{{ button_label|default('Guardar') }}</button>
        <img class="imagenvehiculo" src="{{ asset('images/vehiculo.jpg') }}" alt="Symfony!" />
{{ form_end(form) }}
